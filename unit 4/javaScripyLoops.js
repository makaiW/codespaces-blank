function educationRank(gradelevel) {
    if (gradelevel == 1) {
        console.log("you are a freshmen in undergrad.");

    } else if (gradelevel == 2) {
        console.log("you are a sophmore in undergrad.");
    } else if (gradelevel == 3) {
        console.log("you are a junior in undergrad.");
    } else if (gradelevel == 4) {
        console.log("you are a senoir in undergrad.");
    } else if (gradelevel == 5) {
        console.log("you are in a master programs in gradute school.");
    } else {
        console.log("error. plese check your code.");
    }
    }

    educationRank(3);

// a list is a uniquw grouping data type. that allows
// use to collect and organize data in one variable.

    var codingClass6period =["name,10,true"]
    var student1 = ["will", 90, "football"]
    var student2 = ["jayin",92,"basketballs"]
    var student3 = [ "marquese",80];

    var codingClass6Period =[student1,student2,student3]

    console.log(codingClass6period);
    
    //find the item in the list at index 3 and 1

    var groceryList = ['apple','water','milk','paper towels','dish soap']
    var index1 =["apple"]
    var index2 = [" water"]
    var index3 = ["milk"]
    var index4 = ["paper towel"]

    console.log(groceryList);